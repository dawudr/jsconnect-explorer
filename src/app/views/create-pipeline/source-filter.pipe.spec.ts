import { SourceFilterPipe } from './source-filter.pipe';

describe('SourceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SourceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
